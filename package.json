{
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "lint": "eslint .",
    "test": "pnpm -r test",
    "release": "pnpm -r release && changeset publish",
    "format:check": "prettier --check .",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
